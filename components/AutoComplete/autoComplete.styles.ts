import styled from "styled-components";

export const autoCompleteList = styled.li`
  background-color: ${(props) => props.color};
`;
export const autoCompleteListWrapper = styled.ul``;
export const autoCompleteInput = styled.input``;
